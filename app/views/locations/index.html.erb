 <h3 class="title is-4">Hosts</h3>
 <div class="columns is-mobile is-centered">
<div class="column is-half">
 <table>
      <thead>
      </thead>
      <tbody>
        <% @locations.each do |location| %>
          <tr>
            <td><%= link_to location.area,location%></td>
            <td><%= link_to location.listing.name,@listing%></td>

          </tr>
          <tr>
          </tr>
        <% end %>
      </tbody>
    </table>
    </div>
    </div>