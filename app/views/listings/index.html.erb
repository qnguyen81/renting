<div class="columns is-mobile is-centered">
      <div class="column is-half">
      <h3 class="title is-4">Listings</h3>
       <% @listings.each do |listing| %>
          <div class="columns">
        <div class="column">
         <%= link_to listing.name,listing%>
        </div>
        <div class="column">
         $ <%= link_to listing.price%>
        </div>
      </div>
      <% end %>
      </div>
      </div>